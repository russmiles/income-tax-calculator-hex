/**
 * Application services — orchestrate domain logic to implement use cases.
 *
 * Each service implements one or more inbound ports, uses the domain model,
 * and delegates to outbound ports for I/O. No framework code lives here.
 */
package com.russmiles.incometax.application.service;
