/**
 * Driving (inbound) ports — use case interfaces the application exposes.
 *
 * These are the entry points adapters call into (e.g. CalculateTaxUseCase).
 * Implemented by application services.
 */
package com.russmiles.incometax.application.port.in;
