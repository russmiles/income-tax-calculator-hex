/**
 * Inbound web adapter — translates HTTP requests into use case calls.
 *
 * Controllers, request/response DTOs, and HTTP-specific concerns live here.
 * Depends on inbound ports, never on application services directly.
 */
package com.russmiles.incometax.adapter.in.web;
