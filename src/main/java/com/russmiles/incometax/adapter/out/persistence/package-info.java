/**
 * Outbound persistence adapter — implements outbound ports against a datastore.
 *
 * Repositories, entity mappings, and storage-specific concerns live here.
 * Implements interfaces declared in {@code application.port.out}.
 */
package com.russmiles.incometax.adapter.out.persistence;
